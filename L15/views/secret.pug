doctype html
html(lang="en")
    include includes/header
    body 
        include includes/menubar
        - const user= name
        h1 Welcome #{user} to the secret page
        button(id="clickme") Click me
        label(id="status") Unknown